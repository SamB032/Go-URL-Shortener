apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-script
  namespace: {{ .Values.namespace | quote }}
data:
  create_tables.sql: |-
{{ .Files.Get .Values.migrations.scriptPath | indent 4 }}
